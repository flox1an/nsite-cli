---
phase: 03.1-cross-reference-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/skills/nsyte-concepts/references/nostr-concepts.md
  - .agents/skills/nsyte-auth/SKILL.md
autonomous: true
gap_closure: true
requirements:
  - DEPL-02
  - CONF-03

must_haves:
  truths:
    - "nostr-concepts.md directs agents to 'nsyte bunker connect' (not 'nsyte bunker add') in all occurrences"
    - "nsyte-auth/SKILL.md prerequisites reference nsyte-setup (not nsyte-config) for nsyte init guidance"
    - "An agent following nostr-concepts.md NIP-46 instructions reaches a valid CLI command"
  artifacts:
    - path: ".agents/skills/nsyte-concepts/references/nostr-concepts.md"
      provides: "Correct bunker CLI command references"
      contains: "nsyte bunker connect"
    - path: ".agents/skills/nsyte-auth/SKILL.md"
      provides: "Correct prerequisite cross-reference"
      contains: "nsyte-setup"
  key_links:
    - from: ".agents/skills/nsyte-concepts/references/nostr-concepts.md"
      to: "CLI: nsyte bunker connect"
      via: "inline command reference"
      pattern: "nsyte bunker connect"
    - from: ".agents/skills/nsyte-auth/SKILL.md"
      to: ".agents/skills/nsyte-setup/SKILL.md"
      via: "prerequisite cross-reference"
      pattern: "nsyte-setup"
---

<objective>
Fix incorrect CLI commands and skill cross-references identified by the v1.0 milestone audit.

Purpose: Close DEPL-02 and CONF-03 gaps so that agents following skill documentation reach valid CLI commands and correct prerequisite skills.
Output: Two corrected files with no broken cross-references.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.agents/skills/nsyte-concepts/references/nostr-concepts.md
@.agents/skills/nsyte-auth/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix nsyte bunker command references in nostr-concepts.md</name>
  <files>.agents/skills/nsyte-concepts/references/nostr-concepts.md</files>
  <action>
In `.agents/skills/nsyte-concepts/references/nostr-concepts.md`, replace all occurrences of `nsyte bunker add` with `nsyte bunker connect`. There are exactly two occurrences:

1. **Line 25 (Pubkey section):** Change `nsyte bunker add` to `nsyte bunker connect` in the sentence "the user must run `nsyte init` or `nsyte bunker add` to establish an identity"
2. **Line 59 (NIP-46 / Bunker Auth section):** Change `nsyte bunker add <connection-string>` to `nsyte bunker connect <connection-string>` in the sentence "the user runs `nsyte bunker add <connection-string>`"

Do NOT change any other content. The command `nsyte bunker add` does not exist in nsyte CLI — the correct command is `nsyte bunker connect`.
  </action>
  <verify>
    <automated>grep -c "nsyte bunker add" .agents/skills/nsyte-concepts/references/nostr-concepts.md | grep -q "^0$" && grep -c "nsyte bunker connect" .agents/skills/nsyte-concepts/references/nostr-concepts.md | grep -q "^2$" && echo "PASS" || echo "FAIL"</automated>
    <manual>Read lines 25 and 59 of nostr-concepts.md — both should say "nsyte bunker connect"</manual>
  </verify>
  <done>Zero occurrences of "nsyte bunker add" remain in nostr-concepts.md; exactly two occurrences of "nsyte bunker connect" exist</done>
</task>

<task type="auto">
  <name>Task 2: Fix prerequisite cross-reference in nsyte-auth/SKILL.md</name>
  <files>.agents/skills/nsyte-auth/SKILL.md</files>
  <action>
In `.agents/skills/nsyte-auth/SKILL.md` line 9, change the prerequisite cross-reference from `nsyte-config` to `nsyte-setup`.

Current line 9:
```
- `.nsite/config.json` must exist — run `nsyte init` first (see `nsyte-config` skill)
```

Change to:
```
- `.nsite/config.json` must exist — run `nsyte init` first (see `nsyte-setup` skill)
```

Reason: `nsyte init` is documented in the `nsyte-setup` skill, not `nsyte-config`. The `nsyte-config` skill covers `nsyte config` (TUI editor) and `nsyte validate`, not project initialization.
  </action>
  <verify>
    <automated>grep -n "nsyte init" .agents/skills/nsyte-auth/SKILL.md | grep -q "nsyte-setup" && ! grep -n "nsyte init" .agents/skills/nsyte-auth/SKILL.md | grep -q "nsyte-config" && echo "PASS" || echo "FAIL"</automated>
    <manual>Read line 9 of nsyte-auth/SKILL.md — should reference nsyte-setup skill for nsyte init guidance</manual>
  </verify>
  <done>nsyte-auth/SKILL.md prerequisite line references `nsyte-setup` (not `nsyte-config`) for `nsyte init` guidance</done>
</task>

</tasks>

<verification>
1. `grep -r "nsyte bunker add" .agents/` returns no results
2. `grep "nsyte-config" .agents/skills/nsyte-auth/SKILL.md` returns no hits on the prerequisites line referencing nsyte init
3. `grep "nsyte bunker connect" .agents/skills/nsyte-concepts/references/nostr-concepts.md` returns exactly 2 matches
4. `grep "nsyte-setup" .agents/skills/nsyte-auth/SKILL.md` returns at least 1 match on the prerequisites line
</verification>

<success_criteria>
- Zero occurrences of "nsyte bunker add" in any file under .agents/
- nsyte-auth/SKILL.md prerequisite for nsyte init points to nsyte-setup skill
- No other content changes made to either file
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-cross-reference-fixes/03.1-01-SUMMARY.md`
</output>
