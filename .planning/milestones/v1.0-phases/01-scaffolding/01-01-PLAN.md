---
phase: 01-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/skills/nsyte-setup/SKILL.md
  - .agents/skills/nsyte-deploy/SKILL.md
  - .agents/skills/nsyte-config/SKILL.md
  - .agents/skills/nsyte-auth/SKILL.md
  - .agents/skills/nsyte-ci/SKILL.md
  - .agents/skills/nsyte-concepts/SKILL.md
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02
  - SPEC-05

must_haves:
  truths:
    - "Six skill directories exist under .agents/skills/ with correct names (nsyte-setup, nsyte-deploy, nsyte-config, nsyte-auth, nsyte-ci, nsyte-concepts)"
    - "Every SKILL.md has a name field that is byte-for-byte identical to its parent directory name"
    - "Every SKILL.md has a third-person description with action vocabulary and context vocabulary"
    - "nsyte-concepts SKILL.md has user-invocable: false; nsyte-deploy has disable-model-invocation: true"
  artifacts:
    - path: ".agents/skills/nsyte-setup/SKILL.md"
      provides: "Setup skill entrypoint"
      contains: "name: nsyte-setup"
    - path: ".agents/skills/nsyte-deploy/SKILL.md"
      provides: "Deploy skill entrypoint"
      contains: "name: nsyte-deploy"
    - path: ".agents/skills/nsyte-config/SKILL.md"
      provides: "Config skill entrypoint"
      contains: "name: nsyte-config"
    - path: ".agents/skills/nsyte-auth/SKILL.md"
      provides: "Auth skill entrypoint"
      contains: "name: nsyte-auth"
    - path: ".agents/skills/nsyte-ci/SKILL.md"
      provides: "CI skill entrypoint"
      contains: "name: nsyte-ci"
    - path: ".agents/skills/nsyte-concepts/SKILL.md"
      provides: "Background concepts skill entrypoint"
      contains: "name: nsyte-concepts"
  key_links:
    - from: "directory name"
      to: "SKILL.md name field"
      via: "exact string match"
      pattern: "name: nsyte-(setup|deploy|config|auth|ci|concepts)"
---

<objective>
Create the six skill directories under `.agents/skills/` and write spec-compliant SKILL.md frontmatter stubs for each. No skill body content is written — only the minimal valid frontmatter that will pass `skills-ref validate`.

Purpose: Lock in the directory structure and naming before any body content is written, preventing rework caused by name/directory mismatches discovered after body content exists.
Output: Six SKILL.md files with valid frontmatter, zero body content beyond a placeholder comment.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding/1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skill directories and write SKILL.md frontmatter stubs</name>
  <files>
    .agents/skills/nsyte-setup/SKILL.md
    .agents/skills/nsyte-deploy/SKILL.md
    .agents/skills/nsyte-config/SKILL.md
    .agents/skills/nsyte-auth/SKILL.md
    .agents/skills/nsyte-ci/SKILL.md
    .agents/skills/nsyte-concepts/SKILL.md
    .agents/skills/nsyte-concepts/references/.gitkeep
  </files>
  <action>
Create all directories first, then write each SKILL.md. Use mkdir -p for each:

```bash
mkdir -p .agents/skills/nsyte-setup
mkdir -p .agents/skills/nsyte-deploy
mkdir -p .agents/skills/nsyte-config
mkdir -p .agents/skills/nsyte-auth
mkdir -p .agents/skills/nsyte-ci
mkdir -p .agents/skills/nsyte-concepts/references
```

Then write each SKILL.md using the Write tool (NOT cat/heredoc). Copy directory names verbatim into the `name:` field — do not retype them independently.

**nsyte-setup/SKILL.md:**
```yaml
---
name: nsyte-setup
description: Installs nsyte and verifies the Deno runtime prerequisite. Use when the user wants to install nsyte, check if nsyte is installed, set up nsyte for the first time, or troubleshoot installation on Linux, macOS, or Windows.
---

<!-- Skill body: to be written in Phase 2 -->
```

**nsyte-deploy/SKILL.md:**
```yaml
---
name: nsyte-deploy
description: Deploys a static site to the Nostr network and Blossom servers using nsyte. Use when the user wants to publish, upload, deploy, or host a website on Nostr, Blossom, or a decentralized hosting system using nsyte.
disable-model-invocation: true
---

<!-- Skill body: to be written in Phase 2 -->
```

**nsyte-config/SKILL.md:**
```yaml
---
name: nsyte-config
description: Manages nsyte project configuration including relay URLs, Blossom server URLs, and site metadata. Use when the user wants to configure, update, or validate nsyte settings in .nsite/config.json.
---

<!-- Skill body: to be written in Phase 3 -->
```

**nsyte-auth/SKILL.md:**
```yaml
---
name: nsyte-auth
description: Sets up NIP-46 bunker authentication for nsyte, enabling remote signing without exposing a private key. Use when the user wants to configure bunker auth, connect a remote signer, or set up non-interactive key management for nsyte.
---

<!-- Skill body: to be written in Phase 3 -->
```

**nsyte-ci/SKILL.md:**
```yaml
---
name: nsyte-ci
description: Configures nsyte for non-interactive CI/CD deployment using environment variable authentication and the --ci flag. Use when the user wants to automate nsyte deploys in a CI pipeline, GitHub Actions, or any headless environment.
---

<!-- Skill body: to be written in Phase 3 -->
```

**nsyte-concepts/SKILL.md:**
```yaml
---
name: nsyte-concepts
description: Background knowledge about Nostr protocol, Blossom blob storage, pubkeys, relays, and NIP-46 bunker authentication used by nsyte. Loaded automatically when Nostr or Blossom domain context is needed during nsyte operations.
user-invocable: false
---

<!-- Skill body: to be written in Phase 2. References references/nostr-concepts.md for domain vocabulary. -->
```

Create a `.gitkeep` in `nsyte-concepts/references/` to preserve the directory in git before nostr-concepts.md is written in Plan 02.

**Critical rules:**
- `name` field must use only lowercase letters and hyphens, must start with a letter
- `name` field value must be byte-for-byte identical to the parent directory name
- Do NOT use underscores, uppercase, or add any XML tags to name or description fields
- descriptions must start with a third-person verb (Installs, Deploys, Manages, Sets up, Configures, Background knowledge)
- Do NOT add "When to Use" as a body section — that content belongs only in the description frontmatter field
  </action>
  <verify>
    <automated>
      ls .agents/skills/nsyte-setup/SKILL.md .agents/skills/nsyte-deploy/SKILL.md .agents/skills/nsyte-config/SKILL.md .agents/skills/nsyte-auth/SKILL.md .agents/skills/nsyte-ci/SKILL.md .agents/skills/nsyte-concepts/SKILL.md &amp;&amp; grep -r "^name:" .agents/skills/*/SKILL.md
    </automated>
    <manual>Verify each SKILL.md has a name: field matching its parent directory name exactly. Verify nsyte-deploy has disable-model-invocation: true. Verify nsyte-concepts has user-invocable: false.</manual>
  </verify>
  <done>Six SKILL.md files exist under .agents/skills/, each with a name: field byte-for-byte matching the directory name, and a description starting with a third-person verb. nsyte-deploy has disable-model-invocation: true. nsyte-concepts has user-invocable: false. All files are under 20 lines.</done>
</task>

</tasks>

<verification>
Run after Task 1 completes:

```bash
# Confirm all six files exist
ls .agents/skills/*/SKILL.md

# Confirm name fields match directory names
for d in .agents/skills/*/; do
  skill=$(basename "$d")
  name=$(grep "^name:" "$d/SKILL.md" | head -1 | sed 's/name: //')
  if [ "$skill" = "$name" ]; then
    echo "OK: $skill"
  else
    echo "MISMATCH: dir=$skill name=$name"
  fi
done
```

All six should print OK.
</verification>

<success_criteria>
- Six directories exist under .agents/skills/, one per planned skill
- Each SKILL.md has name: field matching directory name exactly (byte-for-byte)
- Each SKILL.md has a third-person description with action and context vocabulary
- nsyte-deploy has disable-model-invocation: true
- nsyte-concepts has user-invocable: false
- No SKILL.md contains real keys, real relay URLs, or real pubkeys
- nsyte-concepts/references/ directory exists (will hold nostr-concepts.md in Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding/01-01-SUMMARY.md` using the summary template at `@/home/sandwich/.claude/get-shit-done/templates/summary.md`.
</output>
