{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nsyte.run/schemas/config.schema.json",
  "title": "nsyte Configuration Schema",
  "description": "Schema for nsyte project configuration files (.nsite/config.json)",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference for this configuration file",
      "format": "uri"
    },
    "bunkerPubkey": {
      "type": "string",
      "description": "Public key reference for NIP-46 bunker connection",
      "pattern": "^[0-9a-fA-F]{64}$"
    },
    "relays": {
      "type": "array",
      "description": "List of Nostr relay URLs to publish events to",
      "items": {
        "type": "string",
        "format": "uri",
        "pattern": "^wss?://"
      },
      "uniqueItems": true
    },
    "servers": {
      "type": "array",
      "description": "List of Blossom server URLs for file storage",
      "items": {
        "type": "string",
        "format": "uri",
        "pattern": "^https?://"
      },
      "uniqueItems": true
    },
    "publishAppHandler": {
      "type": "boolean",
      "description": "Whether to publish NIP-89 app handler events",
      "default": false
    },
    "publishProfile": {
      "type": "boolean",
      "description": "Whether to publish kind 0 profile metadata events (root sites only)",
      "default": false
    },
    "publishRelayList": {
      "type": "boolean",
      "description": "Whether to publish kind 10002 relay list events (root sites only)",
      "default": false
    },
    "publishServerList": {
      "type": "boolean",
      "description": "Whether to publish kind 10063 Blossom server list events (root sites only)",
      "default": false
    },
    "profile": {
      "type": "object",
      "description": "Profile metadata for kind 0 events",
      "properties": {
        "name": {
          "type": "string",
          "description": "Your name"
        },
        "display_name": {
          "type": "string",
          "description": "Your display name"
        },
        "about": {
          "type": "string",
          "description": "Description about yourself"
        },
        "picture": {
          "type": "string",
          "format": "uri",
          "description": "URL to your avatar image"
        },
        "banner": {
          "type": "string",
          "format": "uri",
          "description": "URL to your banner/header image"
        },
        "website": {
          "type": "string",
          "format": "uri",
          "description": "Your website URL"
        },
        "nip05": {
          "type": "string",
          "description": "Your NIP-05 identifier (e.g., user@domain.com)",
          "pattern": "^[^@]+@[^@]+$"
        },
        "lud16": {
          "type": "string",
          "description": "Your Lightning address (e.g., user@getalby.com)",
          "pattern": "^[^@]+@[^@]+$"
        },
        "lud06": {
          "type": "string",
          "description": "Legacy Lightning address (LNURL)"
        }
      },
      "additionalProperties": false
    },
    "fallback": {
      "type": "string",
      "description": "HTML file to use as 404.html for client-side routing"
    },
    "gatewayHostnames": {
      "type": "array",
      "description": "List of gateway hostnames that can serve this nsite",
      "items": {
        "type": "string",
        "format": "hostname"
      },
      "default": ["nsite.lol"],
      "uniqueItems": true
    },
    "id": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1,
          "maxLength": 13,
          "pattern": "^[a-z0-9-]+$"
        },
        {
          "type": "string",
          "enum": [""]
        },
        {
          "type": "null"
        }
      ],
      "description": "Site identifier for named sites (kind 35128). Must match [a-z0-9-]{1,13} per NIP-5A. Use empty string or null for root site (kind 15128)"
    },
    "title": {
      "type": "string",
      "description": "Optional site title for the manifest event"
    },
    "description": {
      "type": "string",
      "description": "Optional site description for the manifest event"
    },
    "source": {
      "type": "string",
      "description": "Optional repository URL for the source tag in manifest events (http/https only)",
      "format": "uri",
      "pattern": "^https?://"
    },
    "appHandler": {
      "type": "object",
      "description": "NIP-89 app handler configuration",
      "properties": {
        "id": {
          "type": "string",
          "description": "Optional unique identifier for this handler (defaults to site id). Used as the 'd' tag in kind 31990 events.",
          "pattern": "^[a-zA-Z0-9_-]+$"
        },
        "kinds": {
          "type": "array",
          "description": "Event kinds this nsite can handle/display",
          "items": {
            "type": "integer",
            "minimum": 0,
            "maximum": 65535
          },
          "uniqueItems": true
        },
        "name": {
          "type": "string",
          "description": "Optional app name for the handler"
        },
        "description": {
          "type": "string",
          "description": "Optional description of what the app handler does"
        },
        "icon": {
          "type": "string",
          "format": "uri",
          "description": "Optional app icon URL (will be included as 'picture' in the app handlers metadata)"
        },
        "platforms": {
          "type": "object",
          "description": "Platform-specific handler configurations",
          "properties": {
            "web": {
              "type": "object",
              "description": "Web platform handler configuration",
              "properties": {
                "patterns": {
                  "type": "array",
                  "description": "Custom URL patterns for handling entities",
                  "items": {
                    "type": "object",
                    "properties": {
                      "url": {
                        "type": "string",
                        "description": "Full URL pattern (e.g., 'https://example.com/e/<bech32>')",
                        "pattern": "^https?://.+"
                      },
                      "entities": {
                        "type": "array",
                        "description": "Supported entity types for this pattern",
                        "items": {
                          "type": "string",
                          "enum": ["nevent", "naddr", "nprofile", "note", "npub"]
                        }
                      }
                    },
                    "required": ["url"]
                  }
                }
              }
            },
            "android": {
              "type": "string",
              "description": "Android app intent URL or package name"
            },
            "ios": {
              "type": "string",
              "description": "iOS app URL scheme or universal link"
            },
            "macos": {
              "type": "string",
              "description": "macOS app URL scheme or bundle identifier"
            },
            "windows": {
              "type": "string",
              "description": "Windows app protocol or executable path"
            },
            "linux": {
              "type": "string",
              "description": "Linux app command or desktop file"
            }
          }
        }
      },
      "required": ["kinds"],
      "additionalProperties": false
    }
  },
  "required": ["relays", "servers"],
  "additionalProperties": false,
  "examples": [
    {
      "relays": ["wss://relay.damus.io", "wss://nos.lol"],
      "servers": ["https://cdn.hzrd149.com"]
    },
    {
      "relays": ["wss://relay.damus.io"],
      "servers": ["https://cdn.hzrd149.com"],
      "id": "blog",
      "title": "My Blog",
      "description": "A blog about decentralized applications"
    },
    {
      "relays": ["wss://relay.damus.io"],
      "servers": ["https://cdn.hzrd149.com"],
      "publishAppHandler": true,
      "appHandler": {
        "kinds": [1, 30023],
        "name": "My Nostr Viewer",
        "description": "A viewer for notes and articles",
        "icon": "https://example.com/logo.png"
      }
    },
    {
      "relays": ["wss://relay.damus.io", "wss://nos.lol"],
      "servers": ["https://cdn.hzrd149.com"],
      "publishProfile": true,
      "publishRelayList": true,
      "publishServerList": true,
      "profile": {
        "name": "Alice",
        "display_name": "Alice",
        "about": "Decentralization enthusiast",
        "picture": "https://example.com/avatar.jpg",
        "nip05": "alice@example.com",
        "lud16": "alice@getalby.com"
      }
    }
  ]
}
